source 'https://rubygems.org'

# Specify your gem's dependencies in mongoid-paranoia-slug.gemspec

gem 'mongoid', '~> 3.1.0'
gem 'mongoid-paranoia', github: 'simi/mongoid-paranoia', branch: '3.0.0-stable'
gem "mongoid_slug", "~> 2.0.1"

group :test do
  gem 'rspec', '~> 2.12.0'
end

gemspec
